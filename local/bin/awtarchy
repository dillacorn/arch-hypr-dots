#!/usr/bin/env bash
# github.com/dillacorn/awtarchy
# Installed Awtarchy maintenance command and self-updater.

set -Eeuo pipefail
IFS=$'\n\t'
umask 022

REPO_OWNER="dillacorn"
REPO_NAME="awtarchy"
TARGET_USER=""
TARGET_HOME=""
DATA_HOME=""
STATE_HOME=""
INSTALL_DIR=""
RUNTIME=""
COMMAND_VERSION_FILE=""
CONFIG_VERSION_FILE=""

log()  { printf '%s\n' "$*"; }
die()  { printf 'ERROR: %s\n' "$*" >&2; exit 1; }
have() { command -v "$1" >/dev/null 2>&1; }

init_target() {
  if [[ ${EUID} -eq 0 && -n ${SUDO_USER:-} && ${SUDO_USER} != root ]]; then
    TARGET_USER="$SUDO_USER"
    TARGET_HOME="$(getent passwd "$TARGET_USER" | cut -d: -f6 || true)"
  else
    TARGET_USER="${USER:-$(id -un)}"
    TARGET_HOME="${HOME:-}"
  fi

  [[ -n $TARGET_USER ]] || die "Could not determine target user."
  [[ -n $TARGET_HOME && -d $TARGET_HOME ]] || die "Could not determine home directory for ${TARGET_USER}."

  DATA_HOME="${XDG_DATA_HOME:-${TARGET_HOME}/.local/share}"
  STATE_HOME="${XDG_STATE_HOME:-${TARGET_HOME}/.local/state}"
  INSTALL_DIR="${DATA_HOME}/awtarchy"
  RUNTIME="${INSTALL_DIR}/awtarchy-runtime.sh"
  COMMAND_VERSION_FILE="${STATE_HOME}/awtarchy/command-version"
  CONFIG_VERSION_FILE="${STATE_HOME}/awtarchy/config-version"
}

ensure_vpn_directory() {
  local vpn_dir="${TARGET_HOME}/vpn"
  mkdir -p -- "$vpn_dir"
  chmod 0700 "$vpn_dir"
  if [[ ${EUID} -eq 0 ]]; then
    chown "$TARGET_USER:$TARGET_USER" "$vpn_dir"
  fi
}

state_value() {
  local key="$1" file="$2"
  [[ -r $file ]] || return 1
  sed -n "s/^${key}=//p" "$file" | head -n1
}

installed_command_tag() {
  local tag=""
  tag="$(state_value tag "$COMMAND_VERSION_FILE" 2>/dev/null || true)"
  [[ -n $tag ]] && printf '%s\n' "$tag" || printf '%s\n' unknown
}

installed_config_tag() {
  local tag=""
  tag="$(state_value tag "$CONFIG_VERSION_FILE" 2>/dev/null || true)"
  if [[ -z $tag ]]; then
    tag="$(state_value tag "${STATE_HOME}/awtarchy/baseline/metadata" 2>/dev/null || true)"
  fi
  [[ -n $tag ]] && printf '%s\n' "$tag" || printf '%s\n' unknown
}

curl_args() {
  local mode="${1:-api}"
  CURL_ARGS=(
    -fL
    --show-error
    --connect-timeout 5
    --retry 1
    -H "Accept: application/vnd.github+json"
    -H "User-Agent: awtarchy-command"
  )
  case "$mode" in
    api)
      CURL_ARGS+=(--silent --max-time 10)
      ;;
    download)
      CURL_ARGS+=(
        --max-time 300
        --retry 2
        --retry-delay 1
        --speed-limit 1024
        --speed-time 30
        --progress-bar
      )
      ;;
    *) die "Unknown curl mode: ${mode}" ;;
  esac
  if [[ -n ${GITHUB_TOKEN:-} ]]; then
    CURL_ARGS+=(-H "Authorization: Bearer ${GITHUB_TOKEN}")
  fi
}

latest_release_tag() {
  have curl || return 1
  have python3 || return 1
  curl_args api
  local json=""
  json="$(curl "${CURL_ARGS[@]}" "https://api.github.com/repos/${REPO_OWNER}/${REPO_NAME}/releases/latest")" || return 1
  python3 - "$json" <<'PY'
import json
import sys

try:
    payload = json.loads(sys.argv[1])
except (IndexError, json.JSONDecodeError):
    raise SystemExit(1)

tag = str(payload.get("tag_name") or "").strip()
if not tag:
    raise SystemExit(1)
print(tag)
PY
}

urlencode_tag() {
  python3 - "$1" <<'PY'
import sys
from urllib.parse import quote
print(quote(sys.argv[1], safe="-._~"))
PY
}

archive_topdir() {
  python3 - "$1" <<'PY'
import sys
import tarfile
from pathlib import PurePosixPath

with tarfile.open(sys.argv[1], "r:gz") as archive:
    members = archive.getmembers()
    if not members:
        raise SystemExit(1)
    top = ""
    for member in members:
        path = PurePosixPath(member.name)
        if path.is_absolute() or ".." in path.parts:
            raise SystemExit("unsafe archive path")
        if not (member.isfile() or member.isdir()):
            raise SystemExit("unsupported archive entry")
        if path.parts and not top:
            top = path.parts[0]
        elif path.parts and path.parts[0] != top:
            raise SystemExit("archive has multiple top-level paths")
    if not top:
        raise SystemExit(1)
    print(top)
PY
}

atomic_install() {
  local src="$1" dest="$2" mode="$3" dir tmp
  dir="$(dirname "$dest")"
  mkdir -p -- "$dir"
  tmp="$(mktemp --tmpdir="$dir" ".$(basename "$dest").tmp.XXXXXX")"
  install -m "$mode" -- "$src" "$tmp"
  if [[ ${EUID} -eq 0 ]]; then
    chown "$TARGET_USER:$TARGET_USER" "$tmp"
  fi
  mv -Tf -- "$tmp" "$dest"
}

write_command_version() {
  local tag="$1" revision="${2:-}" tmp dir
  dir="$(dirname "$COMMAND_VERSION_FILE")"
  mkdir -p -- "$dir"
  tmp="$(mktemp --tmpdir="$dir" '.command-version.tmp.XXXXXX')"
  {
    printf 'tag=%s\n' "$tag"
    [[ -n $revision ]] && printf 'revision=%s\n' "$revision"
    printf 'updated_at=%s\n' "$(date -Iseconds)"
  } >"$tmp"
  chmod 0644 "$tmp"
  if [[ ${EUID} -eq 0 ]]; then
    chown "$TARGET_USER:$TARGET_USER" "$tmp"
  fi
  mv -Tf -- "$tmp" "$COMMAND_VERSION_FILE"
}

self_update() {
  local tag="${1:-}" tmp archive encoded top repo_dir new_runtime new_launcher revision=""
  have curl || die "curl is required for self-update."
  have tar || die "tar is required for self-update."
  have python3 || die "python3 is required for self-update."
  have sha256sum || die "sha256sum is required for self-update."

  [[ -n $tag ]] || tag="$(latest_release_tag)" || die "Could not determine the latest Awtarchy release."
  encoded="$(urlencode_tag "$tag")"
  tmp="$(mktemp -d)"
  archive="${tmp}/awtarchy.tar.gz"

  curl_args download
  log "Downloading Awtarchy ${tag} (stalled transfers abort after 30 seconds)..."
  if ! curl "${CURL_ARGS[@]}" -o "$archive" \
    "https://github.com/${REPO_OWNER}/${REPO_NAME}/archive/refs/tags/${encoded}.tar.gz"; then
    rm -rf -- "$tmp"
    die "Failed to download release ${tag}."
  fi

  top="$(archive_topdir "$archive")" || {
    rm -rf -- "$tmp"
    die "Release archive validation failed."
  }
  tar -xzf "$archive" -C "$tmp"
  repo_dir="${tmp}/${top}"
  new_runtime="${repo_dir}/local/share/awtarchy/awtarchy-runtime.sh"
  new_launcher="${repo_dir}/local/bin/awtarchy"

  [[ -f $new_runtime ]] || {
    rm -rf -- "$tmp"
    die "Release ${tag} does not contain the Awtarchy runtime."
  }
  [[ -f $new_launcher ]] || {
    rm -rf -- "$tmp"
    die "Release ${tag} does not contain the Awtarchy command."
  }
  bash -n "$new_runtime" || {
    rm -rf -- "$tmp"
    die "Downloaded Awtarchy runtime failed Bash syntax validation."
  }
  bash -n "$new_launcher" || {
    rm -rf -- "$tmp"
    die "Downloaded Awtarchy command failed Bash syntax validation."
  }

  revision="$(sha256sum "$new_runtime" | awk '{print $1}')"
  atomic_install "$new_runtime" "$RUNTIME" 0755
  atomic_install "$new_launcher" "${TARGET_HOME}/.local/bin/awtarchy" 0755
  write_command_version "$tag" "$revision"
  rm -rf -- "$tmp"

  log "Awtarchy command updated to ${tag}."
}

print_version_status() {
  local installed latest="" status="Unknown" config
  installed="$(installed_command_tag)"
  config="$(installed_config_tag)"
  if latest="$(latest_release_tag 2>/dev/null)"; then
    if [[ $installed == "$latest" ]]; then
      status="Up to date"
    elif [[ $installed == unknown || $installed == unreleased ]]; then
      status="Local or untracked command build"
    else
      status="Update available"
    fi
  else
    status="Latest release unavailable"
  fi

  printf 'Command release:   %s\n' "$installed"
  printf 'Config release:    %s\n' "$config"
  printf 'Latest release:    %s\n' "${latest:-unavailable}"
  printf 'Command status:    %s\n' "$status"
}

maybe_offer_update() {
  [[ ${AWTARCHY_SKIP_UPDATE_CHECK:-0} == 1 ]] && return 0
  local installed latest answer=""
  installed="$(installed_command_tag)"
  latest="$(latest_release_tag 2>/dev/null || true)"
  [[ -n $latest ]] || return 0

  if [[ $installed == "$latest" ]]; then
    return 0
  fi
  if [[ $installed == unknown || $installed == unreleased ]]; then
    return 0
  fi

  printf 'Awtarchy command update available: %s -> %s\n' "$installed" "$latest"
  [[ -t 0 && -t 1 && -r /dev/tty && -w /dev/tty ]] || return 0
  printf 'Update the Awtarchy command now? [Y/n] ' >/dev/tty
  IFS= read -r answer </dev/tty || true
  case "$answer" in
    n|N|no|NO) return 0 ;;
    *) self_update "$latest" ;;
  esac
}

require_runtime() {
  [[ -f $RUNTIME ]] || die "Installed runtime is missing: ${RUNTIME}. Run awtarchy-install.sh once to repair it."
}

run_runtime() {
  require_runtime
  bash "$RUNTIME" "$@"
}

exec_runtime() {
  require_runtime
  exec bash "$RUNTIME" "$@"
}

clear_screen() {
  if have clear; then
    clear >/dev/tty 2>/dev/null || clear
  else
    printf '\033[H\033[2J' >/dev/tty 2>/dev/null || printf '\033[H\033[2J'
  fi
}

read_key() {
  local key="" rest=""
  IFS= read -rsn1 key </dev/tty || return 1
  if [[ $key == $'\033' ]]; then
    IFS= read -rsn2 -t 0.05 rest </dev/tty || true
    key+="$rest"
  fi
  printf '%s' "$key"
}

single_select_menu() {
  local title="$1" default_index="$2"
  shift 2
  local -a items=("$@")
  local index="$default_index" key="" i

  while true; do
    clear_screen
    printf '%s\n\n' "$title" >/dev/tty
    for i in "${!items[@]}"; do
      if (( i == index )); then
        printf '  > %s\n' "${items[i]}" >/dev/tty
      else
        printf '    %s\n' "${items[i]}" >/dev/tty
      fi
    done
    printf '\nUp/Down = move, Enter = select, q = quit\n' >/dev/tty
    key="$(read_key || true)"
    case "$key" in
      $'\033[A')
        if (( index > 0 )); then
          ((index--)) || true
        fi
        ;;
      $'\033[B')
        if (( index + 1 < ${#items[@]} )); then
          ((index++)) || true
        fi
        ;;
      $'\n'|$'\r'|"") printf '%s\n' "$index"; return 0 ;;
      q|Q) return 1 ;;
    esac
  done
}

pause_menu() {
  printf '\nPress any key to continue...' >/dev/tty
  read_key >/dev/null || true
}

maintenance_menu() {
  [[ -t 0 && -t 1 && -r /dev/tty && -w /dev/tty ]] || die "The Awtarchy menu requires an interactive terminal."
  maybe_offer_update

  local choice rc
  while true; do
    choice="$(single_select_menu "Awtarchy Maintenance" 0 \
      "Update Awtarchy command" \
      "Update configs (preserve personal modifications)" \
      "Reset configs (clean-slate managed files)" \
      "Review config changes without applying" \
      "Clean Awtarchy backup files" \
      "Version information" \
      "Exit")" || exit 0

    case "$choice" in
      0)
        self_update
        pause_menu
        ;;
      1)
        run_runtime update-reset-backup --mode preserve || rc=$?
        pause_menu
        [[ -z ${rc:-} ]] || return "$rc"
        ;;
      2)
        run_runtime update-reset-backup --mode clean || rc=$?
        pause_menu
        [[ -z ${rc:-} ]] || return "$rc"
        ;;
      3)
        run_runtime update-reset-backup --mode preserve --review-only || rc=$?
        pause_menu
        [[ -z ${rc:-} ]] || return "$rc"
        ;;
      4)
        run_runtime clean-backups || rc=$?
        pause_menu
        [[ -z ${rc:-} ]] || return "$rc"
        ;;
      5)
        clear_screen
        print_version_status >/dev/tty
        pause_menu
        ;;
      *) exit 0 ;;
    esac
    unset rc
  done
}

usage() {
  cat <<'EOF'
Usage:
  awtarchy
  awtarchy update [--tag <tag>] [update options]
  awtarchy reset [--tag <tag>] [update options]
  awtarchy review [--tag <tag>]
  awtarchy clean-backups [options]
  awtarchy version
  awtarchy check-update
  awtarchy self-update [--tag <tag>]

Installation is handled only by awtarchy-install.sh.
EOF
}

main() {
  init_target
  ensure_vpn_directory

  case "${1:-}" in
    version)
      print_version_status
      ;;
    check-update)
      print_version_status
      local installed latest
      installed="$(installed_command_tag)"
      latest="$(latest_release_tag 2>/dev/null || true)"
      if [[ -n $latest && $installed != unknown && $installed != unreleased && $installed != "$latest" ]]; then
        return 10
      fi
      ;;
    self-update)
      shift
      local tag=""
      while (( $# )); do
        case "$1" in
          --tag)
            tag="${2:-}"
            [[ -n $tag ]] || die "--tag requires a release tag."
            shift 2
            ;;
          -h|--help)
            usage
            return 0
            ;;
          *) die "Unknown self-update option: $1" ;;
        esac
      done
      self_update "$tag"
      ;;
    update)
      shift
      maybe_offer_update
      exec_runtime update-reset-backup --mode preserve "$@"
      ;;
    reset)
      shift
      maybe_offer_update
      exec_runtime update-reset-backup --mode clean "$@"
      ;;
    review)
      shift
      maybe_offer_update
      exec_runtime update-reset-backup --mode preserve --review-only "$@"
      ;;
    clean-backups|update-backup-cleaner|backup-cleaner)
      shift
      maybe_offer_update
      exec_runtime clean-backups "$@"
      ;;
    update-reset-backup)
      shift
      maybe_offer_update
      exec_runtime update-reset-backup "$@"
      ;;
    install|dry-run|dryrun|test)
      die "Installation is handled by awtarchy-install.sh, not the awtarchy maintenance command."
      ;;
    help|-h|--help)
      usage
      ;;
    "")
      maintenance_menu
      ;;
    *)
      die "Unknown command: $1"
      ;;
  esac
}

main "$@"
