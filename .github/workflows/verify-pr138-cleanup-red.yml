name: Verify PR138 Cleanup RED

on:
  push:
    branches: [fix/update-terminal-sudo-disk-recovery]

permissions:
  contents: read

jobs:
  red:
    if: github.event.head_commit.message == 'Verify PR138 cleanup RED'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
      - name: Confirm cleanup regression test fails before production change
        run: bash tests/test-awtarchy-update-session.sh