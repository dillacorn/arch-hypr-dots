name: scxctl helper regression

on:
  push:
    branches: [scxctl-helper-update-testing]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
      - run: bash tests/test-scxctl-helper-update.sh
